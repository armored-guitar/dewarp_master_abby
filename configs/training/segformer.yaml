name: segformer_b0_bs3_pretrained_vanila
n_epoch: 100

base_dir: ./results_copy/
logs_dir: logs
checkpoint_dir: checkpoints

mixed_precision: False

optimizer:
  name: adamw
  lr:
    - 0.00004
    - 0.00008
  weight_decay: 1e-10

scheduler:
  name: transformer

